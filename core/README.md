# jmcomic 主服 Core 扩展

| 子目录 | 用途 |
|--------|------|
| `commonconfig/` | 控制台「禁漫本子」（ConfigBase，与 `core/system-Core/commonconfig` 同模式） |
| `plugin/` | QQ `#车牌` |

业务 Python 在插件根目录 `service.py`；运行时配置 `data/jmcomic/config.yaml`。
