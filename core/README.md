# jmcomic 主服 Core 扩展

| 子目录 | 用途 |
|--------|------|
| `commonconfig/` | 控制台「禁漫本子」（ConfigBase） |
| `plugin/` | QQ `#车牌` / `#开盲盒` |
| `workflow/` | AI MCP：`jm_download`（车牌数字）/ `jm_blind_box`（无参） |

业务 Python 在插件根目录 `service.py`；运行时配置 `data/jmcomic/config.yaml`。
